# Full app


## Useful commands

```
git add . && git commit -m "added build script" && git push

```


## Build containers

```
docker build -t "clean_dla:latest" "dla"
```

## Run containers

```
docker compose up -d

docker ps -a
```